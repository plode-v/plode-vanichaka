const Loading = () => {

    return (
        <div className="flex items-center justify-center h-screen">
            <div>
                <h1 className="flex font-epilogue text-[40px] sm:text-[52px] font-bold">Loading...</h1>
            </div>
        </div>
    )
}

export default Loading