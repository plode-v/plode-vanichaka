import { navItems } from "../constants"

const ContactMe = () => {
    return (
        <div className="flex items-center justify-center h-screen border border-black" id={navItems[2].sectionName}><div>Contact Me</div></div>
    )
}

export default ContactMe