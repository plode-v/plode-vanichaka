import React from 'react'
import { navItems } from '../constants'

const Projects = () => {
    return (
        <div className='h-screen flex items-center justify-center' id={navItems[1].sectionName}>
            <div>Project</div>
        </div>
    )
}

export default Projects