import { navItems } from '../constants'

const AboutMe = () => {
    return (
        <div id={navItems[0].sectionName} className='h-[100vh]'>
            AboutMe
        </div>
    )
}

export default AboutMe