import { navItems } from '../constants'

const AboutMe = () => {
    return (
        <div id={navItems[0].sectionName} className='h-[100vh] bg-[#F1F6F9]'>
            AboutMe
        </div>
    )
}

export default AboutMe